// Uso de some:
// ○ Crea un array de números llamado numeros y usa some para
// verificar si algún número es mayor que 10.

const numeros = [1, 2, 3, 4, 11];

const mayor = numeros.some(numero => numero > 10);

console.log(mayor);
